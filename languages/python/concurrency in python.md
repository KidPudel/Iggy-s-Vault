Modern Python have support for ***asynchronous blocks***, that are called [[asyncio coroutines]] with `async` and `await`

In Python concurrency is done via [[Event loop]] which implemented via [[asyncio]]

[[asyncio coroutines]] 

[[GIL]]